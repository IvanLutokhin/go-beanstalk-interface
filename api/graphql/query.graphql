extend type Query {
    server: Server!
    tubes: TubeConnection
    tube(name: String!): Tube!
    job(id: Int!): Job!
}

type TubeConnection {
    edges: [TubeEdge!]!
}

type TubeEdge {
    node: Tube!
}
